"""Report generation for shard membership auditing experiments.

Future phases will implement structured result serialization, human-readable
summary tables, and figure generation (ROC curves, score histograms).
"""


def generate_report(results: dict, output_dir: str = "outputs/reports"):
    raise NotImplementedError("Implemented in Phase 3: report generation.")
